import type { MenuModule } from '@/router/types';

const menu: MenuModule = {
  orderNo: 100,
  menu: {
    name: 'Dashboard',
    path: '/dashboard',
    icon: 'bx:bx-home',
  },
};
export default menu;
